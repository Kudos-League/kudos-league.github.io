/**
 * Maximum kudos that can be granted in a single action — a peer award or a
 * gift/request reward offer. Keep in sync with MAX_KUDOS_PER_AWARD on the
 * server (src/utils/Constants.ts).
 */
export const MAX_KUDOS_PER_AWARD = 1000;
