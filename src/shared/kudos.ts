/**
 * Maximum kudos for a single direct peer award (the Award Kudos flow).
 * Keep in sync with MAX_KUDOS_PER_DIRECT_AWARD on the server.
 */
export const MAX_KUDOS_PER_DIRECT_AWARD = 100;

/**
 * Maximum kudos attached to a single gift/request reward offer.
 * Keep in sync with MAX_KUDOS_PER_OFFER on the server.
 */
export const MAX_KUDOS_PER_OFFER = 1000;
